[package]
name = "hermes_link_embassy_usb"
version = "0.1.0"
edition = "2021"

[dependencies]
embedded-io-async = "0.6"
embassy-usb = "0.4"

derive_more = { version = "2", default-features = false, features = ["from", "deref", "deref_mut"] }
